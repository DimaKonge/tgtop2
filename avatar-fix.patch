[1mdiff --git a/client/src/pages/Home.tsx b/client/src/pages/Home.tsx[m
[1mindex 35cf926..34f178a 100644[m
[1m--- a/client/src/pages/Home.tsx[m
[1m+++ b/client/src/pages/Home.tsx[m
[36m@@ -483,11 +483,29 @@[m [mfunction SettingsSheet({[m
   );[m
 }[m
 [m
[31m-function SafeAvatar({ src, fallback, className = "" }: { src?: string | null; fallback: ReactNode; className?: string }) {[m
[32m+[m[32mfunction SafeAvatar({ src, fallback, className = "", userId }: { src?: string | null; fallback: ReactNode; className?: string; userId?: string | null }) {[m
   const [failed, setFailed] = useState(false);[m
   useEffect(() => setFailed(false), [src]);[m
[31m-  if (!src || failed) return <>{fallback}</>;[m
[31m-  return <img src={src} alt="" className={className} loading="lazy" referrerPolicy="no-referrer" onError={() => setFailed(true)} />;[m
[32m+[m
[32m+[m[32m  // If the primary source fails, and we have a Telegram User ID, try the server-side proxy.[m
[32m+[m[32m  const proxySrc = (failed && userId && src !== `/api/telegram-user-avatar/${userId}`)[m
[32m+[m[32m    ? `/api/telegram-user-avatar/${userId}`[m
[32m+[m[32m    : null;[m
[32m+[m
[32m+[m[32m  const currentSrc = proxySrc || src;[m
[32m+[m
[32m+[m[32m  if (!currentSrc || (failed && !proxySrc)) return <>{fallback}</>;[m
[32m+[m
[32m+[m[32m  return ([m
[32m+[m[32m    <img[m
[32m+[m[32m      src={currentSrc}[m
[32m+[m[32m      alt=""[m
[32m+[m[32m      className={className}[m
[32m+[m[32m      loading="lazy"[m
[32m+[m[32m      referrerPolicy="no-referrer"[m
[32m+[m[32m      onError={() => setFailed(true)}[m
[32m+[m[32m    />[m
[32m+[m[32m  );[m
 }[m
 [m
 function BotAvatar({ username, className = "", imageClassName = "" }: { username: string; className?: string; imageClassName?: string }) {[m
[36m@@ -1907,9 +1925,7 @@[m [mexport default function Home({ onReady }: { onReady?: () => void }) {[m
     typeof window !== "undefined"[m
       ? new URLSearchParams(window.Telegram?.WebApp?.initData ?? "").get("auth_date")[m
       : null;[m
[31m-  const displayUserAvatar = telegramAvatar[m
[31m-    ? `${telegramAvatar}${telegramAvatar.includes("?") ? "&" : "?"}tgtop_avatar=${encodeURIComponent(telegramAvatarVersion ?? "current")}`[m
[31m-    : (user?.avatarUrl || (telegramUserId ? `/api/telegram-user-avatar/${telegramUserId}` : null));[m
[32m+[m
   const telegramFullName = telegramUser[m
     ? [telegramUser.first_name, telegramUser.last_name].filter(Boolean).join(" ").trim()[m
     : "";[m
[36m@@ -1920,10 +1936,22 @@[m [mexport default function Home({ onReady }: { onReady?: () => void }) {[m
     user?.telegramUsername ||[m
     tx("Пользователь Telegram", "Telegram user");[m
   const userTelegramUsername = telegramUser?.username || user?.telegramUsername || null;[m
[32m+[m
   const [userAvatarError, setUserAvatarError] = useState(false);[m
[32m+[m
[32m+[m[32m  const displayUserAvatar = useMemo(() => {[m
[32m+[m[32m    // 1. Prioritize the direct Telegram URL from the Mini App, if it hasn't failed yet.[m
[32m+[m[32m    if (telegramAvatar && !userAvatarError) {[m
[32m+[m[32m      return `${telegramAvatar}${telegramAvatar.includes("?") ? "&" : "?"}tgtop_avatar=${encodeURIComponent(telegramAvatarVersion ?? "current")}`;[m
[32m+[m[32m    }[m
[32m+[m[32m    // 2. If the direct URL failed or is missing, try the proxy or stored avatarUrl.[m
[32m+[m[32m    return (telegramUserId ? `/api/telegram-user-avatar/${telegramUserId}` : null) || user?.avatarUrl || null;[m
[32m+[m[32m  }, [telegramAvatar, userAvatarError, telegramAvatarVersion, telegramUserId, user?.avatarUrl]);[m
[32m+[m
   useEffect(() => {[m
[32m+[m[32m    // Only reset the error state when the underlying user identity changes.[m
     setUserAvatarError(false);[m
[31m-  }, [displayUserAvatar]);[m
[32m+[m[32m  }, [telegramUserId, user?.avatarUrl]);[m
   const userInitial = ([m
     userDisplayName.replace(/^@/, "").trim().slice(0, 1) ||[m
     userTelegramUsername?.replace(/^@/, "").trim().slice(0, 1) ||[m
[36m@@ -3462,7 +3490,7 @@[m [mexport default function Home({ onReady }: { onReady?: () => void }) {[m
                       disabled={!managerPublic || !detail.group.managerUsername}[m
                       className="flex min-h-[38px] w-[76px] shrink-0 items-center gap-1.5 rounded-xl border border-[#354966] bg-[#202b3a] px-2 text-left text-slate-100 transition-colors hover:bg-[#253247] active:scale-[0.98] disabled:cursor-default"[m
                     >[m
[31m-                      <SafeAvatar src={detail.group.managerAvatarUrl} fallback={<UserRound className="h-4 w-4 shrink-0 text-slate-400" />} className="h-5 w-5 shrink-0 rounded-full object-cover" />[m
[32m+[m[32m                      <SafeAvatar src={detail.group.managerAvatarUrl} userId={detail.group.managerTelegramUserId} fallback={<UserRound className="h-4 w-4 shrink-0 text-slate-400" />} className="h-5 w-5 shrink-0 rounded-full object-cover" />[m
                       <b className="truncate text-[9px] leading-3">{detail.group.managerName ?? "Менеджер"}</b>[m
                     </button>}[m
                   </div>[m
[36m@@ -3740,7 +3768,7 @@[m [mexport default function Home({ onReady }: { onReady?: () => void }) {[m
                 <div className="rounded-2xl border border-white/8 bg-[#111720] p-5">[m
                   <div className="flex items-center gap-3">[m
                     <span className="grid h-12 w-12 overflow-hidden rounded-full border border-white/10 bg-[#1b2430] text-sm font-semibold">[m
[31m-                      <SafeAvatar src={publicOwner.owner.avatarUrl} fallback={publicOwner.owner.name?.slice(0, 1).toUpperCase() ?? "T"} className="h-full w-full object-cover" />[m
[32m+[m[32m                      <SafeAvatar src={publicOwner.owner.avatarUrl} userId={publicOwner.owner.openId?.startsWith("telegram:") ? publicOwner.owner.openId.replace("telegram:", "") : null} fallback={publicOwner.owner.name?.slice(0, 1).toUpperCase() ?? "T"} className="h-full w-full object-cover" />[m
                     </span>[m
                     <span className="min-w-0">[m
                       <h1 className="truncate text-lg font-semibold">{publicOwner.owner.name ?? tx("Пользователь TG TOP", "TG TOP user")}</h1>[m
[36m@@ -4153,7 +4181,7 @@[m [mexport default function Home({ onReady }: { onReady?: () => void }) {[m
                     return <button key={entry.owner.openId} onClick={() => openOwner(entry.owner.openId)} className="flex w-full items-center gap-3 px-4 py-3 text-left transition-colors hover:bg-white/[0.025]">[m
                       <span className="w-5 text-center text-xs font-semibold text-[#72a8ff]">{entry.rank}</span>[m
                       <span className="grid h-8 w-8 overflow-hidden rounded-full border border-white/10 bg-[#1b2430] text-[10px] font-semibold">[m
[31m-                        <SafeAvatar src={entry.owner.avatarUrl} fallback={ownerLabel.slice(0, 1).toUpperCase()} className="h-full w-full object-cover" />[m
[32m+[m[32m                        <SafeAvatar src={entry.owner.avatarUrl} userId={entry.owner.openId?.startsWith("telegram:") ? entry.owner.openId.replace("telegram:", "") : null} fallback={ownerLabel.slice(0, 1).toUpperCase()} className="h-full w-full object-cover" />[m
                       </span>[m
                       <span className="min-w-0 flex-1">[m
                         <b className="block truncate text-xs text-slate-200">{ownerLabel}</b>[m
[36m@@ -4617,7 +4645,7 @@[m [mexport default function Home({ onReady }: { onReady?: () => void }) {[m
                   const selected = managerPublic && admin.telegramUserId === selectedManagerTelegramUserId;[m
                   return ([m
                     <button key={admin.telegramUserId} type="button" onClick={() => { setSelectedManagerTelegramUserId(admin.telegramUserId); setManagerPublic(true); }} className={`flex w-full items-center gap-3 rounded-xl border p-3 text-left transition-colors ${selected ? "border-[#3f8cff]/65 bg-[#3f8cff]/12" : "border-white/8 bg-white/[0.025] hover:bg-white/[0.055]"}`}>[m
[31m-                      <span className="grid h-9 w-9 shrink-0 place-items-center overflow-hidden rounded-full border border-white/10 bg-[#1b2430] text-xs font-semibold text-slate-300"><SafeAvatar src={admin.avatarUrl} fallback={admin.name.slice(0, 1).toUpperCase()} className="h-full w-full object-cover" /></span>[m
[32m+[m[32m                      <span className="grid h-9 w-9 shrink-0 place-items-center overflow-hidden rounded-full border border-white/10 bg-[#1b2430] text-xs font-semibold text-slate-300"><SafeAvatar src={admin.avatarUrl} userId={admin.telegramUserId} fallback={admin.name.slice(0, 1).toUpperCase()} className="h-full w-full object-cover" /></span>[m
                       <span className="min-w-0 flex-1"><b className="block truncate text-sm text-slate-100">{admin.name}</b>{admin.username && <small className="mt-0.5 block truncate text-[10px] text-slate-500">@{admin.username}</small>}</span>[m
                       <span className={`grid h-5 w-5 shrink-0 place-items-center rounded-md border ${selected ? "border-[#3f8cff] bg-[#3f8cff] text-white" : "border-white/20 text-transparent"}`}><Check className="h-3.5 w-3.5" /></span>[m
                     </button>[m
[36m@@ -5147,7 +5175,7 @@[m [mexport default function Home({ onReady }: { onReady?: () => void }) {[m
                 <span className="text-[10px] font-medium uppercase tracking-[0.12em] text-slate-500">{tx("Получатель", "Recipient")}</span>[m
                 <div className="mt-2 flex items-center gap-3">[m
                   <span className="grid h-10 w-10 place-items-center overflow-hidden rounded-full bg-[#1b2430] text-sm font-semibold text-slate-300">[m
[31m-                    <SafeAvatar src={reviewedRecipient.avatarUrl} fallback={(reviewedRecipient.name ?? "T").slice(0, 1).toUpperCase()} className="h-full w-full object-cover" />[m
[32m+[m[32m                    <SafeAvatar src={reviewedRecipient.avatarUrl} userId={reviewedRecipient.openId?.startsWith("telegram:") ? reviewedRecipient.openId.replace("telegram:", "") : null} fallback={(reviewedRecipient.name ?? "T").slice(0, 1).toUpperCase()} className="h-full w-full object-cover" />[m
                   </span>[m
                   <span className="min-w-0">[m
                     <b className="block truncate text-sm text-slate-100">{reviewedRecipient.name ?? tx("Пользователь TG TOP", "TG TOP user")}</b>[m
[1mdiff --git a/client/src/pages/Home.user-avatar.test.ts b/client/src/pages/Home.user-avatar.test.ts[m
[1mindex f6cd921..4d498ae 100644[m
[1m--- a/client/src/pages/Home.user-avatar.test.ts[m
[1m+++ b/client/src/pages/Home.user-avatar.test.ts[m
[36m@@ -5,10 +5,9 @@[m [mdescribe("TG TOP Telegram user avatar sync", () => {[m
   it("prioritizes the current Mini App photo_url over a cached session avatar", () => {[m
     const source = readFileSync(new URL("./Home.tsx", import.meta.url), "utf8");[m
 [m
[31m-    expect(source).toContain("const displayUserAvatar = telegramAvatar");[m
[32m+[m[32m    expect(source).toContain("const displayUserAvatar = useMemo");[m
     expect(source).toContain("tgtop_avatar=");[m
     expect(source).toContain("user?.avatarUrl");[m
[31m-    expect(source).not.toContain("user?.avatarUrl ?? telegramAvatar");[m
     expect(source).toContain("src={displayUserAvatar}");[m
     expect(source).toContain("function SafeAvatar");[m
     expect(source).toContain("onError={() => setFailed(true)}");[m
[1mdiff --git a/server/telegramMedia.ts b/server/telegramMedia.ts[m
[1mindex 23ab663..200201c 100644[m
[1m--- a/server/telegramMedia.ts[m
[1m+++ b/server/telegramMedia.ts[m
[36m@@ -135,6 +135,23 @@[m [masync function loadTelegramUserAvatar(userId: string): Promise<TelegramAvatarRes[m
         // Try next token[m
       }[m
     }[m
[32m+[m[32m    // Last resort: try the User Agent (MTProto worker) which has broader access[m
[32m+[m[32m    // than the Bot API for profile photos.[m
[32m+[m[32m    try {[m
[32m+[m[32m      const { fetchTelegramUserProfilePhoto } = await import("./telegramUserAgent");[m
[32m+[m[32m      const body = await fetchTelegramUserProfilePhoto(userId);[m
[32m+[m[32m      if (body) {[m
[32m+[m[32m        const contentType = detectSafeTelegramAvatarContentType(body, "image/jpeg", "avatar.jpg");[m
[32m+[m[32m        if (contentType) {[m
[32m+[m[32m          const result = { kind: "image", body, contentType } as const;[m
[32m+[m[32m          avatarCache.set(cacheKey, result, AVATAR_CACHE_TTL_MS);[m
[32m+[m[32m          return result;[m
[32m+[m[32m        }[m
[32m+[m[32m      }[m
[32m+[m[32m    } catch {[m
[32m+[m[32m      // User Agent not configured or failed[m
[32m+[m[32m    }[m
[32m+[m
     const missing = { kind: "not-found" } as const;[m
     avatarCache.set(cacheKey, missing, AVATAR_NOT_FOUND_CACHE_TTL_MS);[m
     return missing;[m
[1mdiff --git a/server/telegramUserAgent.ts b/server/telegramUserAgent.ts[m
[1mindex 398eed3..cfc2642 100644[m
[1m--- a/server/telegramUserAgent.ts[m
[1m+++ b/server/telegramUserAgent.ts[m
[36m@@ -304,6 +304,22 @@[m [mexport async function fetchTelegramGroupProfileMedia(chatId: string, groupId: nu[m
   }[m
 }[m
 [m
[32m+[m[32mexport async function fetchTelegramUserProfilePhoto(userId: string): Promise<Buffer | null> {[m
[32m+[m[32m  const { client } = await openConnectedTelegramUserAgentClientForWorker();[m
[32m+[m[32m  try {[m
[32m+[m[32m    const media = await client.downloadProfilePhoto(userId);[m
[32m+[m[32m    if (Buffer.isBuffer(media) && media.length > 0) {[m
[32m+[m[32m      await persistConnectedTelegramUserAgentClientSession(client);[m
[32m+[m[32m      return media;[m
[32m+[m[32m    }[m
[32m+[m[32m    return null;[m
[32m+[m[32m  } catch {[m
[32m+[m[32m    return null;[m
[32m+[m[32m  } finally {[m
[32m+[m[32m    await client.disconnect();[m
[32m+[m[32m  }[m
[32m+[m[32m}[m
[32m+[m
 export function encryptTelegramOwnerDmPayload(value: string) {[m
   return encrypt(value);[m
 }[m
