Sticker/emoji first-frame validation: 512×512 sticker frame 0000 shows the complete gold 1–2–4 pyramid with transparent background. The initial 100×100 emoji render is not acceptable: the inherited sticker scale crops the pyramid at the canvas edges. The emoji needs a dedicated smaller scale before delivery.
Transparent asset verification: the corrected 100×100 emoji first frame contains the full 1–2–4 pyramid. Chromium preview shows both sticker 512×512 and emoji 100×100 over a checkerboard with no black background, confirming alpha rendering. Sticker is 75,757 bytes; emoji is 21,907 bytes; both are VP9 WebM, 30 fps, 2 seconds, no audio.
